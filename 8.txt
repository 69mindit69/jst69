import cv2

import numpy as np

image-cv2.imread('/home/cbit/images.jpeg")

angle=45

scale_x=1.5

scale y=1.5

tx=50

ty=50

height, width image.shape[:2]

rotation_matrix-cv2.getRotationMatrix20((width/2,height/2),angle,1)

rotated image-cv2.warpAffine(image, rotation matrix. (width, height))

scaled image cv2.resize(image, None, fx-scale x, fy-scale y)

translation matrix-np.float32(11,0, tx1, 0,1,ty

translated image-cv2.warpAffine(image, rotation matrix, (width, height))

cv2.imshow("Original Inage' image)

cv2.imshow('Rotated Image', rotated image)

cv2.imshow(Scaled image, scaled isage)

cv2.imshow(translated Image,translated image)

cv2.waitKey(0)

cv2.destroyAllWindows()